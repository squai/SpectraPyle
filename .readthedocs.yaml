version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"

sphinx:
  configuration: project_root/docs/conf.py
  fail_on_warning: false

python:
  install:
    - method: pip
      path: project_root
      extra_requirements:
        - docs
